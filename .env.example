# GitHub API Configuration
# -------------------------

# GitHub API token with appropriate permissions (for server mode operation)
# Create at https://github.com/settings/tokens
# Needs repo, admin:org, and user scopes for full functionality
GITHUB_TOKEN=your_token_here

# GitHub username associated with the token
GITHUB_USERNAME=your_username

# Optional: Webhook secret for securing webhooks (if using)
WEBHOOK_SECRET=your_webhook_secret

# OAuth Configuration (for standalone/PWA applications)
# -------------------------

# GitHub OAuth App Client ID
# Create an OAuth App at https://github.com/settings/developers
GITHUB_CLIENT_ID=your_oauth_client_id

# GitHub OAuth App Client Secret
GITHUB_CLIENT_SECRET=your_oauth_client_secret

# Redirect URI for OAuth flow (default: http://localhost:3000/auth/callback)
GITHUB_REDIRECT_URI=http://localhost:3000/auth/callback

# Comma-separated list of OAuth scopes to request
GITHUB_SCOPES=repo,user,admin:org

# Server Configuration
# -------------------------

# Port to listen on
PORT=3000

# Host (localhost for development, set to your domain in production)
HOST=localhost

# Environment (development or production)
NODE_ENV=development

# CORS allowed origins (comma-separated, * for all)
CORS_ORIGINS=*

# Client Application Configuration
# -------------------------

# Default theme (light, dark, or system)
DEFAULT_THEME=light

# Enable offline functionality (true or false)
OFFLINE_ENABLED=true

# Maximum cache age in milliseconds (default: 24 hours)
MAX_CACHE_AGE=86400000

# Enable PWA features (true or false)
PWA_ENABLED=true

# Logging Configuration
# -------------------------

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# Log format (text or json)
LOG_FORMAT=text

# Log to file (true or false)
LOG_TO_FILE=false

# Log file path
LOG_FILE_PATH=logs/app.log
